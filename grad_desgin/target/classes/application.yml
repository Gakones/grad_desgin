spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/typing?serverTimezone=Asia/Shanghai
    username: roots
    password: roots
  thymeleaf:
    cache: false
    #    整合mybatis
mybatis:
  type-aliases-package: com.example.grad_desgin.pojo
  mapper-locations: classpath:mybatis/mapper/*.xml

server:
  port: 8082
